# Alina Framework (SPA, Frontend)

**Front-end part for the main Alina Framework project**
🔗 [ALINA_BE](https://github.com/airstarh/ALINA_BE)

## Side notes:

To run on dev, node.js v20+:

``` bash
NODE_OPTIONS=--openssl-legacy-provider npm run serve:zero
NODE_OPTIONS=--openssl-legacy-provider npm run build:all
```
